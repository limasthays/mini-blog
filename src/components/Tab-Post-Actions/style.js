import styled from 'styled-components'

export const TabPost = styled.div`
  width: fit-content;
  min-width: max-content;

  padding: var(--sm);

  border: 1px solid var(--md-grey);
  background-color: var(--light-grey);
  border-radius: 4px;

  position: absolute;
  margin-top: 1rem;

  span {
    font-size: 14px;
  }
`
